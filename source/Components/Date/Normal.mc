import Toybox.Lang;
using Toybox.Graphics as Gfx;
using Toybox.Time.Gregorian;
using Toybox.WatchUi as Ui;

module Components {
  module Date {
    class Normal extends Ui.Drawable {
      function initialize(params) {
        Drawable.initialize(params);
      }

      function draw(dc) {
      }
    }
  }
}
